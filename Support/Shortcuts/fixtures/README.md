# Shortcut Fixtures

This directory holds golden and failure fixtures for fixed Shortcut contracts.

Naming convention:
- `<contract-id>.ok.json`
- `<contract-id>.error.json`

These fixtures are intended for:
- mirror/query integration tests
- validator tests
- parallel development before live Shortcuts exist
